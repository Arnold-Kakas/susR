% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_susr_helpers.R
\name{resolve_href}
\alias{resolve_href}
\title{Resolve HREF path for a given table and user-supplied dimension parameters}
\usage{
resolve_href(table_code, param_list, table_info_long, lang = "en")
}
\arguments{
\item{table_code}{Character: an 8-character code for the table, e.g. "np3106rr"}

\item{param_list}{List: user-supplied dimension selections, in the order of the table's dimensions}

\item{table_info_long}{A data frame of metadata (long format) for the given table}

\item{lang}{Character: the language code ("en" or "sk"), default "en"}
}
\value{
Character string suitable for appending after the table code in the SUSR dataset endpoint.
}
\description{
This function takes:
\itemize{
\item A table code (e.g. "np3106rr"),
\item A list of parameters (one for each dimension),
\item The \code{table_info_long} metadata (with dimension codes in the needed order),
\item (Optionally) a language code (default "en").
}
}
\details{
It then:
\enumerate{
\item Expands special keywords, such as "last5" or "districts",
\item Checks them against the actual available dimension values via \code{susr_dimension_values()},
\item Discards any invalid codes, warns the user,
\item If no valid codes remain, attempts to use "all" if available, otherwise
the first available value for that dimension, and warns the user.
}

Finally, it returns a single string of the form:
"VAL11,VAL12/.../VALN1,VALN2"
for use in the dataset endpoint call.
}
